using UnityEngine;

public class UselessAsset : MonoBehaviour {

    void Awake ()
    {
    }

	// Use this for initialization
	void Start () {
		this.gameObject.SetActive(false);
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}

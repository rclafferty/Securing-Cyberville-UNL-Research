using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Assets.Scripts.UserSessionLogger;

public class LogStart : MonoBehaviour {

	// Use this for initialization
	void Start () {
        //GameObject.Find("UserSessionLogger").GetComponent<UserSessionLoggerBehavior>().LogToDatabase("Player starts the game", UserSessionLoggerBehavior.UserAction.SceneChange);
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}

using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LoadMayorPic : MonoBehaviour {

	// Use this for initialization
	void Start () {
        /*BillboardAsset ba = new BillboardAsset();
        //Material m = Shader.Find("mayor_pic");
        Material m = (Material)Resources.Load("../Assets/CityHallAssets/inside/mayor_pic.mat", typeof(Material));
        if (m == null)
            Debug.Log("NULL MATERIAL FOR MAYOR");
        ba.material = m;

        BillboardRenderer br = GameObject.Find("Image").GetComponent<BillboardRenderer>();
        br.billboard = ba;*/
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
